#!/bin/bash

THISDIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

echo
echo Removing existing files
rm \
    ~/.gitignore \
    ~/.gemrc \
    ~/.tmux.conf \
    ~/.bash_profile \
    ~/.bashrc \
    ~/.gitconfig \
    ~/.zshrc \
    ~/.eslintrc

echo Making symlinks...

ln -s $THISDIR/.gitignore ~/.gitignore
ln -s $THISDIR/.gemrc ~/.gemrc
ln -s $THISDIR/.tmux.conf ~/.tmux.conf
ln -s $THISDIR/.zshrc ~/.zshrc
ln -s $THISDIR/.eslintrc ~/.eslintrc
ln -s $THISDIR/macos/.bash_profile ~/.bash_profile
ln -s $THISDIR/macos/.bashrc ~/.bashrc
ln -s $THISDIR/macos/.gitconfig ~/.gitconfig

unset THISDIR

echo
echo Done.
echo
