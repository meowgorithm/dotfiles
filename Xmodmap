! To print the current mapping:
!
!   xmodmap -pke
!
! To reset meta keymappings:
!
!   setxkbmap -option
!
! To reset to a US layout:
!
!   setxkbmap -option

! Swap caps lock and escape
!remove Lock = Caps_Lock
!keysym Escape = Caps_Lock
!keysym Caps_Lock = Escape
!add Lock = Caps_Lock

! Map the caps lock key to escape
remove Lock = Caps_Lock
keysym Caps_Lock = Escape

! Map escape to ` (and shift+escape to ~)
keysym Escape = grave asciitilde
